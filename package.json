{
  "name": "section-styler-mvp",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "install:all": "npm --prefix client install && npm --prefix server install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
